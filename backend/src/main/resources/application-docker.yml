spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://postgres:5432/car_rental}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
